version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/spring-boot-cisc-699.jar
  discard-paths: yes
